// let allowedExtensions = /(.md)$/i;
const fs = require('fs');

const isAbsolut = (path) => {
// Función que determina si path es absoluto o relativo, mediante un if-else.
};

// Si y sólo si path es relativo (retorno de función anterior = false), la siguiente función transforma a path en absoluto, sino, contínua como parámetro path normal (ya es absoluto)
const relativeToAbsolut = () => {
};

const isMd = () => {
// Aquí irá función que valida que path (archivo parámetro de mdlink) corresponde a la ruta de un archivo md
// Si es extensión md, entonces retorna true.
// Si no es extensión md, entonces retorna false
};